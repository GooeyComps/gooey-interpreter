import re
import sys
import copy
from stack import *

'''
while tokens:
    if token == '.'
        while first != ',' or '.'
            first = stack.pop()
            
    else:
        add token to stack
        
    
    


'''
depth = 0

def parse(tokens):
    while not s.isEmpty():
        token = stack.pop():
        if token == '.':
            print "found ."
        else:
            

    
def addToParseTree(tree,depth,tokens)